{
  "name": "@flats/common",
  "version": "0.0.0",
  "private": true,
  "license": "",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build:production": "yarn clean && tsc --build tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  }
}
